//
// Font Family
@mixin font($font-family) {
    @if $font-family == regular {
        font-family: "MarkWeb", sans-serif;
    }
    @else if $font-family == bold {
        font-family: "MarkWeb-Bold", sans-serif;
    }
    @else if $font-family == regular--sc {
        font-family: "MarkScWeb", sans-serif;
    }
    @else if $font-family == bold--sc {
        font-family: "MarkScWeb-Bold", sans-serif;
    }
    @else if $font-family == regular--serif {
        font-family: "AlegreyaRegular", sans-serif;
    }
    @else if $font-family == regular--serif--italic {
        font-family: "AlegreyaItalic", sans-serif;
    }
    @else if $font-family == bold--serif {
        font-family: "AlegreyaBold", sans-serif;
    }
    @else if $font-family == bold--serif--italic {
        font-family: "AlegreyaBoldItalic", sans-serif;
    }
}
//
//  Clearfix
@mixin clearfix {
    &:after {
        content: "";
        display: block;
        clear: both;
    }
}
